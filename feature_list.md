1. Allow users to enter fractional angles; the slider would continue with integers
2. The 3D scenes reset when a change is made (HTML). Need to reset this capability.
4. Calculate some specific non-trivial test cases using other code and verify.
5. Test waveplate (mode B) entry.
8. Different color theme. Fix the colors so you can read the legend on the 3d scene
